using System.Collections;
using UnityEngine;
public class Actions : MonoBehaviour
{
    /*
   public IEnumerator CellsMove(GameObject drone,Vector3 movedirection, LayerMask obstaclemask, int cellscount)
    {
        Transform _transform = drone.GetComponent<Transform>();
        RaycastHit hit;
        Ray ray = new Ray(_transform.position, movedirection);
        bool freeinfront = Physics.Raycast(ray,out hit, 1, obstaclemask);
        for(int i = 0; i < cellscount; i++)
        {

        }
    }
    */
}
